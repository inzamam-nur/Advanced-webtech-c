<div style="background-color: skyblue">
    <a href="{{route('home')}}" class="btn btn-primary">Home</a>

    <a href="{{route('products')}}" class="btn btn-primary">Products</a>

    <a href="{{route('ourTeam')}}" class="btn btn-primary">Our Team</a>

    <a href="{{route('contactUs')}}" class="btn btn-primary">Contact us</a>

    <a href="{{route('aboutUs')}}" class="btn btn-primary">About us</a>

    <a href="{{route('studentList')}}" class="btn btn-primary">Student List</a>

    <a href="{{route('teacherCreate')}}" class="btn btn-primary">Register as teacher</a>

    <a href="{{route('teacherList')}}" class="btn btn-primary">Teacher List</a>

    <a href="{{route('login')}}" class="btn btn-primary">Login</a>

    <a href="{{route('studentCreate')}}" class="btn btn-primary">Register</a>

    <a href="{{route('contact')}}" class="btn btn-primary">Contact</a>


</div>